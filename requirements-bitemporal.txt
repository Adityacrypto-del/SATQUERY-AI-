# Bi-temporal deterministic branch (peek/bitemporal-deterministic)
# CPU-only dependencies for build-order steps 1-4 and 7-8.
# Segmentation (step 5-6) deps are listed separately in requirements-segmentation.txt.

numpy>=1.26,<2.2
rasterio>=1.3
affine>=2.4
scipy>=1.11
scikit-image>=0.22
PyYAML>=6.0
pytest>=8.0
Pillow>=10.0
